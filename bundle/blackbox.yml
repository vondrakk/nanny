modules:
  http_2xx:
    prober: http
    timeout: 5s
    http:
      preferred_ip_protocol: ip4
      fail_if_not_ssl: false
      tls_config:
        insecure_skip_verify: true   # internal certs; tighten if you have a real CA
  tcp_connect:
    prober: tcp
    timeout: 5s
