apiVersion: v1
kind: Service
metadata:
  name: {{ include "nanny.fullname" . }}
  labels:
    {{- include "nanny.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "nanny.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
